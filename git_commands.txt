git add mmn2.1.c 
git commit -m "name of commit"
#pull the changes 
git pull --rebase origin master
#merges the conflicts
#commit the changes --amend
git push origin master

#create new branch
#git checkout -b fixbug1

#push to branch
#git push origin fixbug1

